const velocidade = 90;
const limiteVelocidade = 85;

// condicao ? true : false;

const condicao = velocidade >= limiteVelocidade
    ? console.log("Você recebeu uma multa") 
    : console.log("Continua Andando");